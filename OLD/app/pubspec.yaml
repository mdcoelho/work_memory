name: work_memory
description: A local macOS command palette for capturing work memory.
publish_to: "none"

version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hotkey_manager: ^0.2.3
  path: ^1.9.0
  path_provider: ^2.1.5
  sqflite_common_ffi: ^2.3.3
  window_manager: ^0.3.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
